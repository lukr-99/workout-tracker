package com.lukr99.workout.ui.screens

import androidx.compose.runtime.Composable
import com.lukr99.workout.ui.WorkoutViewModel

/**
 * Workout templates — create/edit/reorder (Phase 2; TemplateEditor is a separate flow).
 * Phase 0 stub: title placeholder only.
 */
@Composable
fun TemplatesScreen(vm: WorkoutViewModel) =
    StubScreen("Templates", "Reusable routines")
