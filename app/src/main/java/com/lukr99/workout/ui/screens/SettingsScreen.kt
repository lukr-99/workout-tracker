package com.lukr99.workout.ui.screens

import androidx.compose.runtime.Composable
import com.lukr99.workout.ui.WorkoutViewModel

/**
 * Settings — theme, unit system, rest-timer defaults, import/export entry points (Phases 3+).
 * Phase 0 stub: title placeholder only.
 */
@Composable
fun SettingsScreen(vm: WorkoutViewModel) =
    StubScreen("Settings", "Preferences & data")
