package com.lukr99.workout.ui.screens

import androidx.compose.runtime.Composable
import com.lukr99.workout.ui.WorkoutViewModel

/**
 * Live workout logging — start from scratch/template, set editing, finish/discard (Phase 2).
 * Phase 0 stub: title placeholder only.
 */
@Composable
fun WorkoutScreen(vm: WorkoutViewModel) =
    StubScreen("Workout", "Log a live session")
