package com.lukr99.workout.ui.screens

import androidx.compose.runtime.Composable
import com.lukr99.workout.ui.WorkoutViewModel

/**
 * Home dashboard — active workout, streaks, recent sessions, weekly volume (Phase 4).
 * Phase 0 stub: title placeholder only.
 */
@Composable
fun HomeScreen(vm: WorkoutViewModel) =
    StubScreen("Home", "Your training at a glance")
