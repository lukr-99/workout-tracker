package com.lukr99.workout.ui.screens

import androidx.compose.runtime.Composable
import com.lukr99.workout.ui.WorkoutViewModel

/**
 * History — completed sessions list; open/edit a session (Phase 2; WorkoutDetail is a separate flow).
 * Phase 0 stub: title placeholder only.
 */
@Composable
fun HistoryScreen(vm: WorkoutViewModel) =
    StubScreen("History", "Past workouts")
