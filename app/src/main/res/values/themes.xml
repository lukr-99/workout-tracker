<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window/base theme only; the real UI theme is Compose (WorkoutTheme).
         Edge-to-edge with a dark (light-icon) status bar over the near-black background. -->
    <style name="Theme.WorkoutTracker" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.WorkoutTracker.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.WorkoutTracker</item>
    </style>
</resources>
