<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WorkoutTracker.App.Pages.HomePage"
             Title="Home">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Settings" Clicked="OnSettingsClicked" Priority="0" Order="Primary" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">
                <Border BackgroundColor="{StaticResource Gray950}" Padding="18">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Workout Tracker" FontSize="28" FontAttributes="Bold" />
                        <Label Text="{Binding ActiveWorkoutName}" FontSize="20" FontAttributes="Bold" />
                        <Label Text="{Binding ActiveWorkoutSummary}" TextColor="{StaticResource Gray200}" />
                        <HorizontalStackLayout Spacing="10">
                            <Button Text="Start quick workout" Command="{Binding StartQuickWorkoutCommand}" />
                            <Button Text="Continue" Command="{Binding ContinueWorkoutCommand}" IsEnabled="{Binding HasActiveWorkout}" />
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <Border BackgroundColor="{StaticResource Gray900}" Padding="16">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="Overview" FontSize="18" FontAttributes="Bold" />
                        <Label Text="{Binding AnalyticsSummary}" />
                        <Label Text="{Binding ConsistencySummary}" />
                        <Label Text="{Binding MostLoggedExercise}" />
                    </VerticalStackLayout>
                </Border>

                <Label Text="Recent workouts" FontSize="20" FontAttributes="Bold" />
                <CollectionView ItemsSource="{Binding RecentWorkouts}" SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border BackgroundColor="{StaticResource Gray900}" Padding="14" Margin="0,0,0,10">
                                <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" ColumnSpacing="12">
                                    <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="16" />
                                    <Button Grid.Column="1"
                                            Text="Open"
                                            Style="{StaticResource InlineActionButtonStyle}"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenWorkoutCommand}"
                                            CommandParameter="{Binding .}" />
                                    <Label Grid.Row="1"
                                           Text="{Binding CompletedDateUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}"
                                           TextColor="{StaticResource Gray200}" />
                                    <Label Grid.Row="1"
                                           Grid.Column="1"
                                           Text="{Binding TotalVolumeKg, StringFormat='{}{0:N0} kg'}"
                                           TextColor="{StaticResource Gray200}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
